<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Happiest Valentine</title>

  <!-- Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Parisienne&family=Cormorant+Garamond:wght@400;500&display=swap" rel="stylesheet">

  <style>
    * {
      box-sizing: border-box;
    }

    body {
      margin: 0;
      height: 100vh;
      background: #f6efe6;
      display: flex;
      justify-content: center;
      align-items: center;
      font-family: 'Cormorant Garamond', serif;
      overflow: hidden;
    }

    .heart {
      position: absolute;
      color: #b23a48;
      opacity: 0.4;
      font-size: 16px;
    }

    .card {
      width: 360px;
      max-width: 90%;
      background: #fffaf5;
      border-radius: 32px;
      padding: 60px 40px;
      text-align: center;
      box-shadow: 0 15px 35px rgba(0,0,0,0.12);
      position: relative;
      z-index: 2;
    }

    .bow {
      font-size: 28px;
      margin-bottom: 8px;
    }

    h1 {
      font-family: 'Parisienne', cursive;
      font-size: 48px;
      color: #b23a48;
      margin: 10px 0 18px;
    }

    p {
      font-size: 20px;
      color: #6b4b4b;
      margin-bottom: 26px;
    }

    input {
      width: 100%;
      padding: 12px;
      border-radius: 18px;
      border: 1px solid #d8b4a0;
      font-size: 18px;
      text-align: center;
      outline: none;
      background: #ffffff;
    }

    button {
      margin-top: 18px;
      padding: 10px 30px;
      border-radius: 22px;
      border: none;
      background: #b23a48;
      color: #ffffff;
      font-size: 18px;
      cursor: pointer;
    }

    button:hover {
      background: #922d39;
    }
  </style>
</head>

<body>

<script>
  for (let i = 0; i < 24; i++) {
    const h = document.createElement("div");
    h.className = "heart";
    h.innerText = "❤";
    h.style.left = Math.random() * 100 + "vw";
    h.style.top = Math.random() * 100 + "vh";
    h.style.fontSize = (12 + Math.random() * 14) + "px";
    document.body.appendChild(h);
  }
</script>

<div class="card">
  <div class="bow">🎀</div>
  <h1>Happiest Valentine</h1>
  <p>enter your magic passcode below ;)</p>

  <input id="code" placeholder="♡ your code ♡" />
  <button onclick="go()">enter</button>
</div>

<script>
  const passcodes = {
    "delasomnium": "irene.html",
    "apocalypse": "lerie.html",
    "sunset": "wenju.html",
    "swan": "gistara.html",
    "star": "lilyen.html",
    "pascal": "gataluv.html",
    "winterslight": "viola.html",
    "splits": "vauncover.html",
    "ambassador": "mcfilz.html",
    "ursopretty": "sunshine.html",
    "wokebaddie": "siren.html",
    "biru": "ace.html",
    "monyong": "kimz.html",
    "gacor": "cindy.html",
    "duo": "fortuna.html",
    "sarang": "seonsaengnim.html"
  };

  function go() {
    const val = document.getElementById("code").value.toLowerCase().trim();
    if (passcodes[val]) {
      window.location.href = passcodes[val];
    } else {
      alert("wrong magic code bub 💔");
    }
  }
</script>

<script>
  document.getElementById("code").addEventListener("keydown", function (event) {
    if (event.key === "Enter") {
      go();
    }
  });
</script>

</body>
</html>
